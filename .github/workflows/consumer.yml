name: Terraform Deploy CONSUMER

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  dev-use1:
    uses: ./.github/workflows/reuse-workflow.yml
    with:
      aws-env: dev-use1
