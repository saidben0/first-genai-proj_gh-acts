name: Deploy Infra to AWS Env

on:
  push:
    branches:
      - devv
  pull_request:
    branches:
      - devv

jobs:
  dev-use1:
    uses: ./.github/workflows/tf-common.yml
    with:
      aws-env: dev-use1
